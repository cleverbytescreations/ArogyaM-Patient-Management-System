# ArogyaM PMS — backend runtime dependencies (Stage 0 foundation).
# Pinned with lower bounds; tighten to exact versions once a lockfile is added.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
alembic>=1.13,<2.0
