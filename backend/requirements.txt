# ArogyaM PMS — backend runtime + dev/test dependencies (Stage 0/1 foundation).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
alembic>=1.13,<2.0
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
bcrypt>=3.2.0,<4.0
python-multipart>=0.0.9,<1.0
email-validator>=2.0,<3.0
# optional cache/limiter backend — required when REDIS_URL is set (SEC-T1.2, BE-T1.3)
redis>=5.0,<6.0
# S3-compatible object storage client (MinIO/S3 document binaries)
boto3>=1.34,<2.0
# server-side PDF report rendering (case sheet export — Jinja2 HTML -> WeasyPrint PDF)
Jinja2>=3.1,<4.0
weasyprint>=62.0,<63.0
# pydyf 0.12 removed Stream.transform that weasyprint 62.x relies on (AttributeError
# at render time) — pin to the last release compatible with weasyprint 62.x
pydyf>=0.11,<0.12
# Markdown-to-HTML for investigations/treatments fields in the discharge summary report
markdown>=3.5,<4.0
# testing
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0
# lint / typing (CI gates — configured in pyproject.toml)
ruff>=0.5,<1.0
mypy>=1.10,<2.0
