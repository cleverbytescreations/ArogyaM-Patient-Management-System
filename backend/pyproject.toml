# Tooling configuration for the ArogyaM PMS backend.
# Runtime dependencies are installed from requirements.txt (see Dockerfile);
# this file currently only configures linting/typing per the implementation plan.

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["app"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
# Alembic migrations carry verbatim DDL/seed SQL string literals where the
# 100-col limit harms readability; line-length is not enforced there.
"app/migrations/versions/*.py" = ["E501"]

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]
warn_unused_ignores = true
warn_redundant_casts = true
ignore_missing_imports = true
